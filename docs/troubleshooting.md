# Troubleshooting

## No results appear

Clear active filters or search terms. The UI includes an empty state and reset path for filtered results.

## Content needs to change

The current content source is local mock data under `lib/`. Update it there until a real content backend or CMS is introduced.

## Deployment status is unclear

No deployment-specific config was detected. Confirm the target host before adding deployment instructions.
